# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
import tkinter as tk 
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage


OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"C:\Users\1144008644\Documents\projeto-ihc\Tkinter-Designer-IHC-ALPC\projeto\build\assets\frame0")

import tkinter as tk
import subprocess


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)

def acessar():
    usuario = entry_1.get()
    senha = entry_2.get()
    
    if usuario == "aluno" and senha == "123":
        def run_python_file():
            file_path = "C:/Users/1144008644/Documents/projeito-ihc-pt2/Tkinter-Designer-IHC-ALPC/projeto2/build/gui.py"  # Replace with the path to your Python file

            # Execute o arquivo Python usando subprocess
            try:
                subprocess.run(['python', file_path], check=True)
            except subprocess.CalledProcessError as e:
                result_text.set(f"Erro ao executar o arquivo: {str(e)}")
            else:
                result_text.set("Arquivo Python executado com sucesso!")

            # Cria a janela principal do Tkinter
            root = tk.Tk()
            root.title("Executar Arquivo Python")

            # Label para exibir o resultado da execução
            result_text = tk.StringVar()
            result_label = tk.Label(root, textvariable=result_text)
            result_label.pack(pady=10)

            # Botão para executar o arquivo Python
            run_button = tk.Button(root, text="Executar Python", command=run_python_file)
            run_button.pack(pady=10)

            root.mainloop()
        print("Logou!Bem vindo!")
        run_python_file()
    else:
        print("Login inválida!")

window = Tk()

window.geometry("430x932")
window.configure(bg = "#FFFFFF")


canvas = Canvas(
    window,
    bg = "#FFFFFF",
    height = 932,
    width = 430,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
image_image_1 = PhotoImage(
    file=relative_to_assets("image_1.png"))
image_1 = canvas.create_image(
    215.0,
    466.0,
    image=image_image_1
)

entry_image_1 = PhotoImage(
    file=relative_to_assets("entry_1.png"))
entry_bg_1 = canvas.create_image(
    215.5,
    400.5,
    image=entry_image_1
)
entry_1 = Entry(
    bd=0,
    bg="#FFFFFF",
    fg="#000716",
    highlightthickness=0
)
entry_1.place(
    x=84.0,
    y=376.0,
    width=263.0,
    height=47.0
)
#entry_1.config(***)
canvas.create_text(
    75.0,
    361.0,
    anchor="nw",
    text="E-mail",
    fill="#000000",
    font=("Inter", 12 * -1)
)

button_image_1 = PhotoImage(
    file=relative_to_assets("button_1.png"))
button_1 = Button(
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    #command=lambda: print("button_1 clicked"),
    command=acessar,
    relief="flat"
)
button_1.place(
    x=143.0,
    y=607.0,
    width=144.0,
    height=67.0
)

entry_image_2 = PhotoImage(
    file=relative_to_assets("entry_2.png"))
entry_bg_2 = canvas.create_image(
    215.5,
    510.5,
    image=entry_image_2
)
entry_2 = Entry(
    bd=0,
    bg="#FFFFFF",
    fg="#000716",
    highlightthickness=0
)
entry_2.place(
    x=84.0,
    y=486.0,
    width=263.0,
    height=47.0
)

canvas.create_text(
    75.0,
    471.0,
    anchor="nw",
    text="Senha",
    fill="#000000",
    font=("Inter", 12 * -1)
)

canvas.create_text(
    223.0,
    539.0,
    anchor="nw",
    text="Esqueceu sua senha?",
    fill="#000000",
    font=("Inter", 13 * -1)
)
window.resizable(False, False)
window.mainloop()